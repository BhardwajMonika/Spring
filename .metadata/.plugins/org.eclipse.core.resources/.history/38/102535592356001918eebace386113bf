//it will aslo be used for binding from data to the model using @ModelAttribute annotation in controller
package com.niit.crm.model;

import javax.persistence.*;

@Entity
@Table(name = "customer")
public class Customer {

	@Id
	@GeneratedValue(strategy = GenerationType.IDENTITY)
	@Column(name = "id")
	private int id;
	
}
